# ParallelI_image_processing
A project implement Parallel Image processing with discrete Laplace operator , using both CPU parallel and GPU paralle
Keep only report because the copy right of professor.
